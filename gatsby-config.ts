import type { GatsbyConfig } from "gatsby"

const config: GatsbyConfig = {
  siteMetadata: {
    title: "React D3 Calendar Heatmap",
  },
  plugins: [],
  pathPrefix: `/react-d3-calendar-heatmap`,
}

export default config